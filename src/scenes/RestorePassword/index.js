import React, { memo } from 'react'
import { View, Text } from 'react-native'

export const RestorePassword = memo(function() {
  return (
    <View>
      <Text>restore pass</Text>
    </View>
  )
})
