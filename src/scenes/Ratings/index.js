import React, { memo } from 'react'
import { Text } from 'react-native'

import { ContainerWithBackground } from '@components/ContainerWithBackground'

export const Ratings = memo(function() {
  return (
    <ContainerWithBackground>
      <Text>ratings</Text>
    </ContainerWithBackground>
  )
})
