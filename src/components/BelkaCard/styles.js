import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  // card: {
  //   padding: 10,
  //   borderRadius: 20
  // },
  gradient: {
    // width: '40%',
    padding: 10,
    borderRadius: 20
  }
})
