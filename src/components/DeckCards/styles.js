import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  deck: {
    position: 'absolute',
    top: 50,
    left: 10,
    width: 150,
    height: 50,
    backgroundColor: 'cyan'
  }
})
