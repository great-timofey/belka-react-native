import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  controlsContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 40,
  },
  controlsContainerSingleControl: {
    justifyContent: 'center',
  },
})
